const { create } = require('./create');
const { fetch } = require('./fetch');
const { deleteRide } = require('./delete');
const { update } = require('./update');

module.exports = {
  create,
  fetch,
  deleteRide,
  update,
};
